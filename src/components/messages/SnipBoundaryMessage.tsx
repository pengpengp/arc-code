/**
 * SnipBoundaryMessage — UI marker for snipped conversation boundaries.
 * Stub for HISTORY_SNIP flag.
 */

import React from 'react'

export function SnipBoundaryMessage() {
  return <div>[Conversation history compacted]</div>
}
